# Finler Frontend (Vite + React + Tailwind)

## Setup
```bash
cd frontend
npm install
cp .env.sample .env
npm run dev
```

You can point the app at your backend by editing `.env`:
```env
VITE_API_BASE=http://localhost:8000
VITE_INVEST_API_KEY=DEMO_KEY_123
```
